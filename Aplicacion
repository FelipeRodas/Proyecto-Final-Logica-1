import numpy as np
from Torneo import Torneo
from Usuario import Usuario
from Equipo import Equipo
from funciones import *

class Aplicacion:
    
    #Atributos Publicos
    
    usuarios = np.ndarray
    cont_usuarios = int()
    torneos = np.ndarray
    cont_torneos = int()
    equipos = np.ndarray
    cont_equipos = int()
    #Constantes
    
    MAX_USUARIOS = 100
    ARCHIVO_USUARIOS = "datos_usuarios.npy"
    MAX_TORNEOS = 20
    ARCHIVO_TORNEOS =  "datos_torneos.npy"
    MAX_EQUIPOS = 20 
    ARCHIVO_EQUIPOS = "datos_equipos.npy"
    
    
    def __init__(self):
        """
        Este constructor carga los archivos de datos que contienen los datos almacenados de la aplicación
        """

        # Carga los usuarios almacenados en el archivo de usuarios
        self.usuarios, self.cont_usuarios = self.cargar_datos(self.ARCHIVO_USUARIOS, self.MAX_USUARIOS)

        # Si no hay usaurios se crea un ADMIN por defecto
        if (self.cont_usuarios == 0):
            rol = 1 
            nickname = "felipe"
            documento="12345"
            contra= "felipe"
            self.usuarios[0] = Usuario(rol,nickname, documento,contra)
            self.cont_usuarios = 1
            print("Se cargo el ADMIN por defecto")
            # Guarda en el archivo los datos de los usuarios
            if (not self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)):
                print("No se pudo guardar el archivo de usuarios")
            else:
                print("Se actualizó el archivo de usuarios")
            
        
        #Carga los torneos almacenados en el archivo de torneos
        self.torneos, self.cont_torneos = self.cargar_datos(self.ARCHIVO_TORNEOS, self.MAX_TORNEOS)
        
        #Si no hay torneos se crea un Torneo por defecto
        if (self.cont_torneos ==0):
            self.torneos[0] = Torneo(nombre="Torneo1")
            self.cont_torneos = 1
            print("Se cargo el Torneo por defecto")
            # Guarda en el archivo los datos de los torneos
            if (not self.guardar_datos(self.torneos, self.ARCHIVO_TORNEOS)):
                print("No se pudo guardar el archivo de torneos")
            else:
                print("Se actualizó el archivo de torneos")
                
                
        #Carga los Equipos almacenados en el archivo de equipos
        self.equipos, self.cont_equipos = self.cargar_datos(self.ARCHIVO_EQUIPOS, self.MAX_EQUIPOS)
        
        
    def cargar_datos(self, archivo, num_max_datos):
        """ Este método carga los datos de un archivo, en un arreglo específico

            PARAMS
                archivo = URL relativa del archivo a abrir
                num_max_datos = indica el tamaño máximo de datos que almacena el arreglo

            RETURNS
                arreglo_de_datos = arreglo con los datos cargados
                num_datos = cantidad de datos cargados en el arreglo
        """
        try:
            arreglo_de_datos = np.load(archivo, allow_pickle=True)
            i = 0
            while (arreglo_de_datos[i] != None):
                i += 1
            return arreglo_de_datos, i
        except (FileNotFoundError, EOFError):
            print (f"No se pudo cargar el archivo {archivo}. Se creó un arreglo de datos vacío!")
            arreglo_de_datos = np.full((num_max_datos), fill_value=None, dtype=object)
            return arreglo_de_datos, 0
        
        
    def guardar_datos(self, arreglo_de_datos, archivo):
        """ Este método almacena los datos de un arreglo en un archivo

            PARAMS
                arreglo_de_datos = arreglo Numpy con los datos a alamcenar
                archivo = URL relativa del archivo en el que se almacenarán los datos

            RETURNS
                True si almacena los datos correctamente en el archivo
                False si no logra almacenar los datos en el archivo
        """
        try:
            np.save(archivo, arreglo_de_datos)
            return True
        except (FileNotFoundError, EOFError):
            print (f"Error: no se pudieron almacenar los  datos en el archivo {archivo}.")
            return False
        
    
    def registrar_usuario(self,usuario):
        """
        Este método recibe como parametro obligatorio un objeto dde tipo Usuario
        y da solución al requerimiento registrar usuario.
        Este metodo no tiene Return.
        
        """
        if (self.cont_usuarios < self.MAX_USUARIOS):
            
            # Almacena el usuario creado al arreglo y aumenta el contador de usuarios registrados en 1
            self.usuarios[self.cont_usuarios] = usuario
            self.cont_usuarios += 1
            print("El usuario se registró con éxito")

            # Guarda en el archivo los datos de los usuarios
            if (not self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)):
                print("No se pudo guardar el archivo de usuarios")
            else:
                print("Se actualizó el archivo de usuarios")

        else:
            print("Arreglo de usuarios lleno, no se pueden almacenar más usaurios")
            
          
    def crear_jugador(self):
        """
        Este metodo no recibe parametros y su funcion es crear un objeto de la 
        clase Usuario con atributos que lo definan como jugador(rol == 3)
        Este metodo no tiene Return
        
        """
        
        while True:
            rol = 3
            Jugador1 = Usuario(rol)
            nickname = input("Ingrese el nickname del jugador que desea crear: ")
            flag = False

            # Recorre solo los Usuarios ya creados
            for j in range(self.cont_usuarios):
                if self.usuarios[j].nickname == nickname:
                    flag = True
                    break

            if flag:
                print("---------------------------")
                print("El nickname ingresado ya está registrado en otro Usuario")
                print("---------------------------")
            else:
                break

        Jugador1.pedir_datos_jugador(nickname)
        self.registrar_usuario(Jugador1)    
        
        
    def eliminar_jugador(self):
        """
        Este metodo no recibe parametros y su funcion es eliminar un objeto de la 
        clase Usuario con atributos que lo definan como jugador(rol == 3)
        Este metodo no tiene Return
        
        """
        while True:
            nickname = input("Ingrese el nickname del Jugador que desea eliminar: ")
            flag = False
            pos = -1
            
            # Buscar el Jugador por nickname
            for j in range(self.cont_usuarios):
                if self.usuarios[j].nickname == nickname :
                    flag = True
                    pos = j
                    break
        
                
                if not flag:
                    print("---------------------------")
                    print("No existe un Jugador con ese nickname")
                    print("---------------------------")
                else:
                    if (self.usuarios[pos].rol == 3):
                        # Desplazar los Jugadores siguientes una posición hacia la izquierda
                        for k in range(pos, self.cont_usuarios-1):
                            self.usuarios[k] = self.usuarios[k + 1]
                    
                        # Vaciar la última posición, porque la ultima posicion se duplica
                        self.usuarios[self.cont_usuarios - 1] = None 
                    
                        # Actualizar contador
                        self.cont_usuarios -= 1
                        print("Jugador eliminado correctamente")
                        print("---------------------------")
                    else:
                        print("---------------------------")
                        print("No existe un Jugador con ese nickname")
                        print("---------------------------")
                        
                
                # Guarda en el archivo los datos de los usuarios
                if (not self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)):
                    print("No se pudo guardar el archivo de usuarios")
                else:
                    print("Se actualizó el archivo de usuarios")
            
                    break
                
            
            else :
                print("No existe un jugador con ese nickname")
           
    def registrar_torneo(self,torneo):
        """
        
       Este método recibe como parametro obligatorio un objeto dde tipo Torneo
       y da solución al requerimiento registrar Torneo.
       Este metodo no tiene Return.
        
        """
        if (self.cont_torneos < self.MAX_TORNEOS):
            
            # Almacena el torneo creado al arreglo y aumenta el contador de torneos registrados en 1
            self.torneos[self.cont_torneos] = torneo
            self.cont_torneos += 1
            print("El torneo se registró con éxito")

            # Guarda en el archivo los datos de los torneos
            if (not self.guardar_datos(self.torneos, self.ARCHIVO_TORNEOS)):
                print("No se pudo guardar el archivo de torneos")
            else:
                print("Se actualizó el archivo de torneos")

        else:
            print("Arreglo de usuarios lleno, no se pueden almacenar más usaurios")
    
    def crear_torneo(self):
        """
        Este metodo no recibe parametros y su funcion es crear un objeto de la 
        clase Torneo con atributos.
        Este metodo no tiene Returns
        
        """
        
        while True:
            Torneo1 = Torneo()
            nombre = input("Ingrese el nombre del torneo que desea crear: ")
            flag = False

            # Recorre solo los torneos ya creados
            for j in range(self.cont_torneos):
                if self.torneos[j].nombre == nombre:
                    flag = True
                    break

            if flag:
                print("---------------------------")
                print("El nombre ingresado ya está registrado en otro torneo")
                print("---------------------------")
            else:
                break

        Torneo1.pedir_datos(nombre)
        self.registrar_torneo(Torneo1)
    
    def eliminar_torneo(self):
        """
        Este metodo no recibe parametros y su funcion es eliminar un objeto de la 
        clase Torneo
        Este metodo no tiene Return
        
        """
        while True:
            nombre = input("Ingrese el nombre del torneo que desea eliminar: ")
            flag = False
            pos = -1
            
            # Buscar el torneo por nombre
            for j in range(self.cont_torneos):
                if self.torneos[j].nombre == nombre:
                    flag = True
                    pos = j
                    break
            
            if not flag:
                print("---------------------------")
                print("No existe un torneo con ese nombre")
                print("---------------------------")
            else:
                # Desplazar los torneos siguientes una posición hacia la izquierda
                for k in range(pos, self.cont_torneos-1):
                    self.torneos[k] = self.torneos[k + 1]
            
                # Vaciar la última posición, porque la ultima posicion se duplica
                self.torneos[self.cont_torneos - 1] = None 
            
                # Actualizar contador
                self.cont_torneos -= 1
                print("Torneo eliminado correctamente")
                print("---------------------------")
            
            # Guarda en el archivo los datos de los torneos
            if (not self.guardar_datos(self.torneos, self.ARCHIVO_TORNEOS)):
                print("No se pudo guardar el archivo de torneos")
            else:
                print("Se actualizó el archivo de torneos")
                break
            
    def crear_Organizador(self):
        """
        Este metodo no recibe parametros y su funcion es crear un objeto de la 
        clase Usuario con atributos que lo definan como Organizador(rol == 2)
        Este metodo no tiene Return
        
        """
        
        while True:
            rol = 2
            organizador1 = Usuario(rol)
            nickname = input("Ingrese el nickname del Organizador que desea crear: ")
            flag = False

            # Recorre solo los Usuarios ya creados
            for j in range(self.cont_usuarios):
                if self.usuarios[j].nickname == nickname:
                    flag = True
                    break

            if flag:
                print("---------------------------")
                print("El nickname ingresado ya está registrado en otro Usuario")
                print("---------------------------")
            else:
                break

        organizador1.pedir_datos_Organizador(nickname)
        self.registrar_usuario(organizador1)
        
    def eliminar_Organizador(self):
        """
        Este metodo no recibe parametros y su funcion es eliminar un objeto de la 
        clase Usuario con atributos que lo definan como jugador(rol == 3)
        Este metodo no tiene Return
        
        """
        while True:
            nickname = input("Ingrese el nickname del Organizador que desea eliminar: ")
            flag = False
            pos = -1
            
            # Buscar el Organizador por nickname
            for j in range(self.cont_usuarios):
                if self.usuarios[j].nickname == nickname :
                    flag = True
                    pos = j
                    break
            if (self.usuarios[pos].rol == 2):
                
                if not flag:
                    print("---------------------------")
                    print("No existe un Organizador con ese nickname")
                    print("---------------------------")
                else:
                    # Desplazar los Usuarios siguientes una posición hacia la izquierda
                    for k in range(pos, self.cont_usuarios-1):
                        self.usuarios[k] = self.usuarios[k + 1]
                
                    # Vaciar la última posición, porque la ultima posicion se duplica
                    self.usuarios[self.cont_usuarios - 1] = None 
                
                    # Actualizar contador
                    self.cont_usuarios -= 1
                    print("Organizador eliminado correctamente")
                    print("---------------------------")
                
                # Guarda en el archivo los datos de los usuarios
                if (not self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)):
                    print("No se pudo guardar el archivo de usuarios")
                else:
                    print("Se actualizó el archivo de usuarios")
            
                    break
            else :
                print("No existe un jugador con ese nickname")
                
                
    def Menu_Administrador(self):
        print("Ingrese 1. para gestionar Torneos")
        print("Ingrese 2. para gestionar Organizadores")
        print("Ingese 3. para gestionar Jugadores")
        print("Ingrese 0 para Salir de Sistema")
        opc2 = input("Ingrese una opcion: ")
        print("---------------------------")
        opc2 = Verificacion_Opcion(opc2)
        match opc2:
            case "1":
                print("GESTION DE TORNEOS")
                print("Ingrese 1. para crear un Torneo")
                print("Ingrese 2. para eleminar un Torneo")
                print("Ingrese 3. para modificar el estado de un Torneo")
                print("Ingrese 4. para ver resultados de un Torneo")
                print("Ingrese 0. Si desea salir del sistema")
                opc3 = input("Ingrese una opcion: ")
                opc3 = Verificacion_Opcion(opc3)
                match opc3:
                    case "1":
                        print("CREACION DE TORNEOS")
                        resp = "0"
                        while resp =="0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i + 1, self.torneos[i].nombre)
                            self.crear_torneo()
                            print("Ingrese 0 para CREAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                                
                    case "2":
                        print("ELIMINACION DE TORNEOS")
                        resp = "0"
                        while resp == "0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i+1,self.torneos[i].nombre)
                            self.eliminar_torneo()
                            print("Ingrese 0 para ELIMINAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                    
                    case "3":
                        print("MODIFICACION DE TORNEOS")
                        resp = "0"
                        while resp == "0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i+1,self.torneos[i].nombre)
                            nombre = input("Ingrese el nombre del torneo que desea modificar: ")
                            for i in range(self.cont_torneos):
                                if self.torneos[i].nombre == nombre:
                                    self.torneos[i].Cambiar_estado(self.usuarios,self.cont_usuarios)
                                    self.guardar_datos(self.torneos, self.ARCHIVO_TORNEOS)
                                    print("Se ha guardado la informacion del torneo en la base de datos")
                            print("Ingrese 0 para MODIFICAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                    
                    case "4":
                        self.ver_resultados_torneo()
                          
                    case "0":
                        print("GRACIAS POR USAR LA APLICACION.")
            
            case "2":
                print("GESTION DE ORGANIZADORES")
                print("Ingrese  1  para agregar Organizador")
                print("Ingrese 2 o 3 para eliminar Organizador")
                print("Ingrese 0 Si desea salir del sistema")
                opc = input("Ingrese una opcion: ")
                opc = Verificacion_Opcion(opc)
                match opc:
                    case "1":
                        print("CREACION DE ORGANIZADORES")
                        resp = "0"
                        for i in range(self.cont_usuarios):
                            if self.usuarios[i].rol == 2:
                                print(i+1,self.usuarios[i].nickname)
                        while resp =="0":
                            self.crear_Organizador()
                            print("Ingrese 0 para CREAR otro Organizador")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                    case "2" | "3":
                        print("ELIMINACION DE ORGANIZADORES")
                        resp = "0"
                        for i in range(self.cont_usuarios):
                            if self.usuarios[i].rol == 2:
                                print(i+1,self.usuarios[i].nickname)
                        while resp == "0":
                            self.eliminar_Organizador()
                            print("Ingrese 0 para ELIMINAR otro Orgaanizador")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                
                    case "0":
                        print("GRACIAS POR USAR LA APLICACION.")
                       
            case "3":
                print("GESTION DE JUGADORES")
                print("Ingrese  1  para agregar jugador")
                print("Ingrese 2 o 3 para eliminar jugador")
                print("Ingrese 0 Si desea salir del sistema")
                opc = input("Ingrese una opcion: ")
                opc = Verificacion_Opcion(opc)
                match opc:
                    case "1":
                        print("CREACION DE JUGADORES")
                        resp = "0"
                        print("Lista de Jugadores")
                        for i in range(self.cont_usuarios):
                            if self.usuarios[i].rol == 3:
                                print(i+1,self.usuarios[i].nickname)
                        while resp =="0":
                            self.crear_jugador()
                            print("Ingrese 0 para CREAR otro Jugador")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                    case "2" | "3":
                        print("ELIMINACION DE JUGADORES")
                        resp = "0"
                        for i in range(self.cont_usuarios):
                            if self.usuarios[i].rol == 3:
                                print(i+1,self.usuarios[i].nickname)
                        while resp == "0":
                            
                            self.eliminar_jugador()
                            print("Ingrese 0 para  ELIMINAR otro Jugador")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                   
                    case "0":
                        print("GRACIAS POR USAR LA APLICACION.")
            case "0":
                print("GRACIAS POR USAR LA APLICACION.")
                
    
    def Menu_Organizador(self):
        print("Ingrese 1. para gestionar Torneos")
        print("Ingese 2 o 3. para gestionar Jugadores")
        print("Ingrese 0 para Salir de Sistema")
        opc2 = input("Ingrese una opcion: ")
        print("---------------------------")
        opc2 = Verificacion_Opcion(opc2)
        match opc2:
            case "1":
                print("GESTION DE TORNEOS")
                print("Ingrese 1. para crear un Torneo")
                print("Ingrese 2. para eleminar un Torneo")
                print("Ingrese 3. para modificar el estado de un Torneo")
                print("Ingrese 4. para ver resultados de un Torneo")
                print("Ingrese 0. Si desea salir del sistema")
                opc3 = input("Ingrese una opcion: ")
                opc3 = Verificacion_Opcion(opc3)
                match opc3:
                    case "1":
                        print("CREACION DE TORNEOS")
                        resp = "0"
                        while resp =="0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i+1,self.torneos[i].nombre)
                            self.crear_torneo()
                            print("Ingrese 0 para CREAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                                
                    case "2":
                        print("ELIMINACION DE TORNEOS")
                        resp = "0"
                        while resp == "0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i+1,self.torneos[i].nombre)
                            self.eliminar_torneo()
                            print("Ingrese 0 para ELIMINAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                    
                    case "3":
                        print("MODIFICACION DE TORNEOS")
                        resp = "0"
                        while resp == "0":
                            print("---LISTA DE TORNEOS CREADOS---")
                            for i in range(self.cont_torneos):
                                print(i+1,self.torneos[i].nombre)
                            nombre = input("Ingrese el nombre del torneo que desea modificar")
                            for i in range(self.cont_torneos):
                                if self.torneos[i].nombre == nombre:
                                    self.torneos[i].Cambiar_estado(self.usuarios,self.cont_usuarios)
                                    self.guardar_datos(self.torneos, self.ARCHIVO_TORNEOS)
                            print("Ingrese 0 para MODIFICAR otro Torneo")
                            print("Ingrese Cualquier caracter diferente de (0) para Salir")
                            resp = input("Ingrese una opcion: ")
                    
                    case "4":
                        self.ver_resultados_torneo()
                          
                    case "0":
                        print("GRACIAS POR USAR LA APLICACION.")
                
                
            case "2" | "3":
                print("GESTION DE JUGADORES")
                print("Ingrese  1  para agregar jugador")
                print("Ingrese 2 o 3 para eliminar jugador")
                print("Ingrese 0 Si desea salir del sistema")
                opc = input("Ingrese una opcion: ")
                opc = Verificacion_Opcion(opc)
                match opc:
                    case "1":
                        print("CREACION DE JUGADORES")
                        resp = "0"
                        while resp =="0":
                            self.crear_jugador()
                            print("Ingrese Cualquier caracter diferente de (1) para CREAR otro Jugador")
                            print("Ingrese 1 para Salir")
                            resp = input("Ingrese una opcion: ")
                            
                    case "2" | "3":
                        print("ELIMINACION DE JUGADORES")
                        resp = "0"
                        while resp == "0":
                            self.eliminar_Organizador()
                            print("Ingrese Cualquier caracter diferente de (1) para ELIMINAR otro Jugador")
                            print("Ingrese 1 para Salir")
                            resp = input("Ingrese una opcion: ")
                   
                    case "0":
                        print("GRACIAS POR USAR LA APLICACION.")
                
            case "0":
                print("GRACIAS POR USAR LA APLICACION.")
    
    def Menu_Jugador(self,usuario):
        opcion = "1"
        while opcion == "1":
            usuario = usuario
            print("Ingrese 1 para INSCRIBIRSE en un Torneo.")
            print("Ingrese 2 para  SALIRSE de un Torneo.")
            print("Ingrese 3 para MODIFICAR sus datos.")
            print("Ingrese 0 Si desea salir del sistema")
            opc = input("Ingrese una opcion: ")
            print("---------------------------")
            opc1 = Verificacion_Opcion(opc)
            match opc1:
                case "1":
                    print("---LISTA DE TORNEOS EN LO QUE SE PUEDE INSCRIBIR---")
                    for i in range(self.cont_torneos):
                        if self.torneos[i].estado == 0:
                            print(i+1 ,self.torneos[i].nombre," Modalidad: ",self.torneos[i].modalidad)
                            
                    print("En apartado de Modalidad (1) Es individual, (2) Es Por equipos")
                    aux = 1
                    while aux == 1 :
                        name = input("Ingrese el nombre del torneo al que desea incribirse: ")
                        flag = False
                        for j in range(self.cont_torneos):
                            if (self.torneos[j].nombre == name):
                                if (self.torneos[j].estado == 0):
                                    pos = 0
                                    for i in range(self.cont_usuarios):
                                        if self.usuarios[i] == usuario:
                                            bandera_registro = False
                                            for k in range(self.usuarios[i].cont_inscritos):
                                                if self.usuarios[i].name_T[k] == name:
                                                    flag = True
                                                    pos = i
                                                    bandera_registro = True
                                                    break
                                                    
                                                else:
                                                    bandera_registro = False
                                                    
                                                      
                                            if bandera_registro ==False:    
                                                if self.usuarios[i].cont_inscritos < 5:
                                                   self.usuarios[i].name_T[self.usuarios[i].cont_inscritos] = name
                                                   self.usuarios[i].cont_inscritos += 1
                                                   flag = True
                                                   pos = i
                                    if flag == True:
                                        
                                        if  (self.torneos[j].modalidad == "2"):
                                            print("El torneo es de modalidad por equipos")
                                            equipo = input("Ingrese el nombre del equipo donde desea participar: ")
                                            bandera_equipo = False
                                            pos_equipo = -1

                                            for m in range(self.cont_equipos):
                                                if self.equipos[m] is not None and self.equipos[m].nombre == equipo:
                                                    bandera_equipo = True
                                                    pos_equipo = m
                                                    break

                                            if bandera_equipo:
           

                                                for i2 in range(self.cont_usuarios):
                                                    if self.usuarios[i2] == usuario:
                                                        self.usuarios[i2].name_E = equipo
                                                        break

                                                cont_actual = 0
                                                for j2 in range(len(self.equipos[pos_equipo].jugadores)):
                                                    if self.equipos[pos_equipo].jugadores[j2] is not None:
                                                        cont_actual += 1

                                                cont_actual = self.equipos[pos_equipo].agregar_jugador(cont_actual)

                                                if (not self.guardar_datos(self.equipos, self.ARCHIVO_EQUIPOS)):
                                                    print("No se pudo guardar el archivo de equipos")
                                                else:
                                                    print("Se actualizó el archivo de equipos")
                                                    print("Te has inscrito correctamente al Torneo por equipos:", self.torneos[j].nombre)

                                                aux = 0
                                            else:
                                                print("No hay ningun equipo con ese nombre")
                                                print("¿Desea crear un equipo nuevo?")
                                                print("Ingrese 1. para crear un equipo")
                                                print("Ingrese un caracter diferente de 1 para Salir")
                                                opcion = input("Ingrese una opcion: ")
                                                if opcion == "1":
                                                    equipo1 = Equipo()
                                                    equipo1.crear_equipo()
                                                    equipo1.jugadores = np.full((5), fill_value=None, dtype=object)
                                                    cont_actual = 0
                                                    cont_actual = equipo1.agregar_jugador(cont_actual)
                                                    self.equipos[self.cont_equipos] = equipo1
                                                    self.cont_equipos += 1
                                                    self.guardar_datos(self.equipos, self.ARCHIVO_EQUIPOS)
                                                aux = 0
                                        else: 
                                            print("Te has inscrito correctamente al Torneo:",self.torneos[j].nombre)
                                            self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)
                                            print("Actualmente estas Inscrito en estos Torneo")
                                            for i in range(self.usuarios[pos].cont_inscritos):
                                                print(self.usuarios[pos].name_T[i])
                                                
                                            aux = 0
                                            print("1. Si desea continuar en el MENU")
                                            print("Pulse un caracter diferente de (1) para SALIR")
                                            opcion = input("Ingrese una opcion: ")
                                            print("---------------------------")
                                            print("---------------------------")
                                             
                                else:
                                    print("El torneo ingresado no esta en la lista de Torneos disponibles.")
                        if flag == False:
                            print("NO hay ningun torneo con el nombre que se ingreso")
                case "2": 
                    name = input("Ingrese el nombre del torneo del que desea SALIRSE:")
                    for i in range(self.cont_usuarios):
                        if self.usuarios[i] == usuario:
                            bandera_registro = False
                            pos = 0
                            for k in range(self.usuarios[i].cont_inscritos):
                                if self.usuarios[i].name_T[k] == name:
                                    pos = k
                                    bandera_registro = True
                                    break
                                
                            if bandera_registro == False:
                                print("---------------------------")
                                print("Actualmente no estas inscrito en un Torneo con ese nombre")
                                print("---------------------------")
                                
                            else: 
                                # Desplazar los Torneo inscritos siguientes una posición hacia la izquierda
                                for j in range(pos, self.usuarios[i].cont_inscritos-1):
                                    self.usuarios[i].name_T[j] = self.usuarios[i].name_T[j+1]
                                    
                                # Vaciar la última posición, porque la ultima posicion se duplica
                                self.usuarios[i].name_T[self.usuarios[i].cont_inscritos-1] = None 
                            
                                # Actualizar contador
                                self.usuarios[i].cont_inscritos = self.usuarios[i].cont_inscritos - 1
                                print("Te has salido correctamente del Torneo")
                                print("---------------------------")
                            
                            print("Actualmente estas Inscrito en estos Torneo")
                            for k in range(self.usuarios[i].cont_inscritos):
                                print(self.usuarios[i].cont_inscritos,self.usuarios[i].name_T[k])
                                
                    self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)
                    print("Ya no eres participante del torneo")
                    print("1. Si desea continuar en el MENU")
                    print("Pulse un caracter diferente de (1) para SALIR")
                    opcion = input("Ingrese una opcion: ")
                    print("---------------------------")
                    print("---------------------------")
                     
        
                case "3":
                    print("Modificacion de datos.")
                    print("1. Para modificar su nickname.")
                    print("2. Para modificar su clave.")
                    print("3. Para modificar su nombre.")
                    print("0. Para Salir")
                    opc = input("Ingrese una opcion: ")
                    print("---------------------------")
                    opc = Verificacion_Opcion(opc)
                    match opc:
                        case "1":
                            while True:
                                name_cambio = input("Ingrese el nuevo nickname: ")
                                flag = False
                                pos = 0
                                for i in range(self.cont_usuarios):
                                    if self.usuarios[i].nickname == name_cambio:
                                        flag = True
                                    if self.usuarios[i] == usuario:
                                        pos = i
                                        
                                if flag:
                                    print("El nickname ya esta registrado en otro Usuario")
                                        
                                else:
                                    self.usuarios[pos].nickname = name_cambio
                                    self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)
                                    print("El nickname ha sido actualizado.")
                                    break
                            print("1. Si desea continuar en el MENU")
                            print("Pulse un caracter diferente de (1) para SALIR")
                            opcion = input("Ingrese una opcion: ")
                            print("---------------------------")
                            print("---------------------------")
                                        
                        case "2":
                            clave_cambio = input("Ingrese la nueva clave: ")
                            pos = 0
                            for i in range(self.cont_usuarios):
                                if self.usuarios[i] == usuario:
                                    pos = i
                                        
                            self.usuarios[pos].contra = clave_cambio
                            self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)
                            print("La clave ha sido actualizada.")
                            print("1. Si desea continuar en el MENU")
                            print("Pulse un caracter diferente de (1) para SALIR")
                            opcion = input("Ingrese una opcion: ")
                            print("---------------------------")
                            print("---------------------------")
                            
                        case "3":
                            nombre_cambio = input("Ingrese el nombre: ")
                            for i in range(self.cont_usuarios):
                                if self.usuarios[i] == usuario:
                                    pos = i
                            self.usuarios[pos].nombre = nombre_cambio
                            self.guardar_datos(self.usuarios, self.ARCHIVO_USUARIOS)
                            print("El nombre ha sido actualizado.")
                            print("1. Si desea continuar en el MENU")
                            print("Pulse un caracter diferente de (1) para SALIR")
                            opcion = input("Ingrese una opcion: ")
                            print("---------------------------")
                            print("---------------------------")
                            
                        case "0":
                            print("GRACIAS POR USAR LA APLICACION.")
                            opcion = "0"
                            
                            
                
                case "0":
                    print("GRACIAS POR USAR LA APLICACION.")
                    opcion = "0"

    # NUEVO METODO SOLO DE CONSULTA DE RESULTADOS
    def ver_resultados_torneo(self):
        print("---LISTA DE TORNEOS---")
        for i in range(self.cont_torneos):
            if self.torneos[i] is not None:
                print(i + 1, self.torneos[i].nombre)

        nombre = input("Ingrese el nombre del torneo que desea consultar: ")

        pos = -1
        for i in range(self.cont_torneos):
            if self.torneos[i] is not None and self.torneos[i].nombre == nombre:
                pos = i
                break

        if pos == -1:
            print("No existe un torneo con ese nombre")
            return

        print("Estado del torneo:", self.torneos[pos].estado)
        print("Partidas registradas:")
        for p in self.torneos[pos].partidas:
            if p is not None:
                print(p.contrincante1, "vs", p.contrincante2, "- estado:", p.estado, "- resultado:", p.resultado)
    
    def Principal(self):
        resp = 0
        while(resp == 0):
            limpiar_consola()
            print("B I E N V E N I D @  A LA APP DEL TORNEO")
            print(" --------------------------")
            print("|0. Iniciar seccion        |")
            print("---------------------------")
            print("|1.Registrarse             |")
            print("---------------------------")
            print("|2 o 3. Para Salir         |")
            print("---------------------------")
            opc = input("Ingrese una opcion: ")
            print("---------------------------")
            opc1 = Verificacion_Opcion(opc)
            match opc1:
                case "0":
                    ing = "0"
                    
                    while ing == "0" :
                        nickname = input("Ingrese el usuario para iniciar seccion: ")
                        clave = input("Ingrese la clava para iniciar seccion: ")
                        flag =False
                        for i in range(self.cont_usuarios):
                            if (self.usuarios[i].nickname == nickname and self.usuarios[i].contra == clave):
                                print("Se inicio secion correctamente.")
                                rol = self.usuarios[i].rol
                                usuario = self.usuarios[i]
                                match rol:
                                    case 1:
                                        flag = True
                                        input("Presione ENTER para continuar ...")
                                        limpiar_consola()
                                        print("Accediste como ADMINISTRADOR")
                                        self.Menu_Administrador()
                                        ing = "1"
                                        
                                        
                                    case 2:
                                        flag = True
                                        input("Presione ENTER para continuar ...")
                                        limpiar_consola()
                                        print("Accediste como ORGANIZADOR")
                                        self.Menu_Organizador()
                                        ing = "1"
                                    case 3:
                                        flag = True
                                        input("Presione ENTER para continuar ...")
                                        limpiar_consola()
                                        print("Accediste como JUGADOR")
                                        self.Menu_Jugador(usuario)
                                        ing = "1"
                                        
                        if flag == False:   
                            print("---------------------------")
                            print("El Usuario o la clave ingresada son incorrectos")
                            print("¿Desea volver a intertarlo?")
                            print("0 o 1 Para ingresar el usuario y la clave nuevamente")
                            print("2 o 3. Para Salir")
                            ing = input("Ingrese una opcion: ")
                            ing = Verificacion_Opcion(ing)
                             
                case "1":
                    
                    self.crear_jugador()
                case "2"|"3":
                    print("SALIENDO DEL SISTEMA...")
                    print("Gracias por usar el App Torneos")
                    resp = 1
                    

mi_torneo = Aplicacion()
mi_torneo.Principal()
